{
    "version": 2,
    "builds": [
      {
    
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "out",
          "installCommand": "npm install"
        }
      }
    ],
    "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/index.html"
      }
    ]
  }
